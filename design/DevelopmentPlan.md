# DevelopmentPlan

## 项目名称

**Notifyra**

## 文档目的

本文档基于已确认的技术方案，输出 V1 开发任务拆解、阶段划分、里程碑定义与工期估算，用于后续开发执行与进度追踪。

技术基线（已确认）：
- 数据库：SQLite，文件位于 `/data/app.db`
- 文件存储：`/data/avatars/`
- 调度：`node-schedule`
- 队列：V1 本地内存任务投递
- 技术栈：Node.js + TypeScript

---

## 开发阶段总览

| 阶段 | 内容 | 建议工期 |
|---|---|---|
| 阶段一 | 基础能力：项目初始化、数据库、用户注册登录 | 3 天 |
| 阶段二 | 渠道核心：渠道管理 CRUD、渠道驱动 | 2 天 |
| 阶段三 | 通知核心：通知 CRUD、渠道绑定、状态管理 | 3 天 |
| 阶段四 | 执行层：调度器、Worker、渠道发送、重试 | 3 天 |
| 阶段五 | 推送记录：记录写入、列表、详情 | 1 天 |
| 阶段六 | 外部调用：Webhook 触发、渠道调用、Token 管理 | 2 天 |
| 阶段七 | AI 创建：多轮会话、通知生成 | 2 天 |
| 阶段八 | 测试模块：双入口、示例代码生成 | 2 天 |
| 阶段九 | 个人设置：资料、密码、AI 配置、时间偏好 | 1 天 |
| 阶段十 | 前端：页面串联、交互串联、样式 | 5 天 |
| 阶段十一 | 联调与修复：全流程联调、Bug 修复 | 3 天 |

**预计总工期：约 27 个工作日**

---

## 阶段一：基础能力

### 任务编号：D-1.1
**任务内容**：项目初始化
- 初始化 Node.js + TypeScript 项目
- 配置 tsconfig、eslint、prettier
- 搭建分层目录结构（auth / notify / channel / record / test / settings / ai）
- 引入依赖：Express / Prisma / node-schedule / better-sqlite3 等
- 初始化 Prisma + SQLite，配置数据库路径 `/data/app.db`
- 初始化 `/data/` 目录结构和 `/data/avatars/` 头像目录

### 任务编号：D-1.2
**任务内容**：用户注册登录
- 用户注册接口（用户名唯一性校验、最小长度）
- 用户登录接口（密码校验、Token 签发）
- 登录 Token 鉴权中间件
- 全局 user_id 注入与数据隔离
- 退出登录
- 用户表初始化（bcrypt 密码哈希）

### 任务编号：D-1.3
**任务内容**：个人设置（基础）
- 头像上传与存储（本地 `/data/avatars/`）
- 头像 URL 返回
- 修改密码
- 用户信息查询 / 更新

---

## 阶段二：渠道核心

### 任务编号：D-2.1
**任务内容**：渠道管理 CRUD
- 渠道表设计与 Prisma model
- 新增渠道（name、type、configJson、retryCount）
- 渠道列表查询（分页、搜索、类型筛选、状态筛选）
- 渠道详情
- 渠道编辑
- 渠道启停（PATCH status）
- 渠道删除（含引用检查）
- 渠道调用 Token 生成与哈希存储

### 任务编号：D-2.2
**任务内容**：渠道驱动层
- 统一 Driver 接口定义
- 企业微信 Webhook 驱动
- 飞书 Webhook 驱动
- 钉钉 Webhook 驱动
- Bark 驱动
- 通用 Webhook 驱动
- PushPlus 驱动
- 驱动注册与路由表

---

## 阶段三：通知核心

### 任务编号：D-3.1
**任务内容**：通知管理 CRUD
- 通知表设计与 Prisma model
- 通知与渠道关联表
- 新建通知（支持三种触发类型）
- 通知列表（分页、搜索、类型筛选、状态筛选）
- 通知详情（条件展示 Webhook 信息）
- 通知编辑（重算 nextTriggerAt）
- 通知启停（含可用渠道检查）
- 通知删除

### 任务编号：D-3.2
**任务内容**：通知 Token 管理
- Webhook 通知 Token 生成（仅生成时明文返回一次）
- Token 哈希存储
- Token 重置接口
- Token 哈希比对校验

### 任务编号：D-3.3
**任务内容**：状态与停用逻辑
- 通知状态机实现（active / disabled / blocked_no_channel / completed）
- 渠道停用时通知联动检查
- 通知自动停用原因记录（stop_reason）
- 通知启用前置检查（至少一个可用渠道）

---

## 阶段四：执行层

### 任务编号：D-4.1
**任务内容**：调度器
- node-schedule 集成
- 定时扫描（每分钟）到期的一次性 / 周期性通知
- 任务投递（本地内存队列或简易 job 表）
- 防重复执行（乐观锁 / for update skip locked）
- 周期性通知 nextTriggerAt 自动重算
- 一次性通知执行后状态置为 completed

### 任务编号：D-4.2
**任务内容**：Worker 执行
- 任务消费逻辑
- 通知配置加载（标题、正文、渠道列表）
- 逐渠道发送循环
- 重试策略实现（读取渠道 retryCount，0/10s/30s 退避）
- 部分成功判定逻辑
- 执行结果写入推送记录

### 任务编号：D-4.3
**任务内容**：Webhook 触发
- 外部 Webhook 接收路径
- Token 解析与通知定位
- 原始请求快照保存（body、来源 IP、时间）
- 正文模板渲染（`{{body.xxx}}`，缺失保留原占位符）
- 触发后任务投递与执行

### 任务编号：D-4.4
**任务内容**：模板渲染
- 模板解析器实现
- body JSON 路径读取
- 缺失字段保留占位符
- 防注入限制

---

## 阶段五：推送记录

### 任务编号：D-5.1
**任务内容**：推送记录写入与查询
- 推送记录表设计与 Prisma model
- 渠道结果表
- Webhook 请求日志表
- 推送记录列表（默认 10 条、分页、筛选）
- 推送记录详情（含条件展示 Webhook 原始请求）
- 概览统计（通知数、成功数、失败数、最近推送记录）

---

## 阶段六：外部调用

### 任务编号：D-6.1
**任务内容**：通知级 Webhook 调用
- 固定调用路径实现
- Token 哈希校验
- 请求处理与任务投递
- 来源标记（source=webhook）

### 任务编号：D-6.2
**任务内容**：渠道级直连调用
- 固定调用路径实现
- 渠道 Token 哈希校验
- 直接发送逻辑
- 推送记录写入（source=channel_api）

---

## 阶段七：AI 创建

### 任务编号：D-7.1
**任务内容**：AI 会话管理
- AI 会话表设计与 Prisma model
- 创建 AI 会话
- 消息历史追加
- 会话状态机（collecting / ready_to_create / completed / failed）

### 任务编号：D-7.2
**任务内容**：AI 多轮创建
- 读取用户 AI 配置（Base URL、API Key、Model）
- 未配置 AI 时的提示逻辑
- 多轮对话编排（汇总 collected_params）
- 参数齐全判断
- 创建通知落库
- 会话状态同步

---

## 阶段八：测试模块

### 任务编号：D-8.1
**任务内容**：测试通知入口
- 选择已有通知
- 读取通知当前配置
- 执行测试发送（走 Worker，但标记 source=test_notification）
- 返回测试结果

### 任务编号：D-8.2
**任务内容**：测试渠道入口
- 选择已有渠道
- 填写测试标题和正文
- 直接调用渠道驱动（不走通知）
- 返回测试结果

### 任务编号：D-8.3
**任务内容**：示例代码生成
- curl / JavaScript / Python 三种语言
- 按测试对象切换示例代码内容
- 通知调用示例 vs 渠道调用示例区分

---

## 阶段九：个人设置完善

### 任务编号：D-9.1
**任务内容**：时间偏好
- 下午 / 晚上 / 明早默认时间配置
- 配置读取与更新

### 任务编号：D-9.2
**任务内容**：AI 配置
- AI 配置读取与更新（Base URL / API Key / Model）
- API Key 加密存储
- 测试连接接口

---

## 阶段十：前端

### 任务编号：D-10.1
**任务内容**：前端基础
- 项目初始化（React + TypeScript + Vite）
- 路由配置
- 状态管理（React Query）
- 请求封装（Token 注入、错误处理）
- Tailwind CSS + 组件库
- 左侧导航组件（高级玻璃感）
- 登录 / 注册页面

### 任务编号：D-10.2
**任务内容**：核心页面
- 概览页（统计卡片 + 快捷入口 + 最近推送）
- 通知列表页（switch / 图标 / 分页）
- 新建通知页（AI 区 + 手动表单 + 触发类型联动）
- 通知详情页（Webhook 条件展示）
- 渠道列表页（warning 提示条）
- 渠道详情页（调用 Token 展示）
- 推送记录列表页（顶部统计数据）
- 推送记录详情页（Webhook 原始请求条件展示）
- 测试模块页（Tab 双入口）
- 个人设置页

### 任务编号：D-10.3
**任务内容**：前端交互细节
- Token 明文一次展示弹窗（关闭后脱敏）
- Cron 表达式输入校验
- 日期时间选择器
- AI 多轮对话界面
- 示例代码复制

---

## 阶段十一：联调与修复

### 任务编号：D-11.1
**任务内容**：全流程联调
- 端到端流程测试
- 注册 → 新建渠道 → 新建通知 → 调度执行 → 查看推送记录
- Webhook 触发流程测试
- AI 多轮创建流程测试
- 测试模块全流程测试

### 任务编号：D-11.2
**任务内容**：Bug 修复与收尾
- 修复联调发现的问题
- 异常场景处理（网络异常、配置错误、Token 失效等）
- 前后端错误提示统一
- README 补充运行说明
- 渠道兼容修复记录：`design/dev/ChannelCompatibilityFix-2026-04-04.md`
  - 说明：本次已完成渠道 type / config key 历史兼容、新数据收敛、前端展示统一，以及相关测试与类型检查收尾

### 渠道兼容修复补充记录（2026-04-04）
- 背景：渠道测试发送 `/test/channel/:id/send` 曾因历史渠道 `type` 与 driver registry 的 canonical type 不一致而返回“暂不支持该渠道类型”。
- 问题范围：不仅是 `type` 不一致，历史配置 key 也与后端 driver 实际要求不一致；若只修 type，不修 config key，发送链路仍会在 driver 层失败。
- 实施方向：
  - 后端在 channels 模块集中做 type / config 归一化，兼容历史数据并收敛新写入。
  - 前端 API、表单、列表、详情、通知关联展示统一改为 canonical type 与真实配置字段。
  - 前端抽取共享渠道展示映射，统一列表、详情、测试模块、通知页面的类型文案与筛选来源。
  - 验证范围覆盖登录态测试发送、开放 token 发送、渠道筛选展示与相关单测。
- 关联文档：
  - 详细修复记录：`design/dev/ChannelCompatibilityFix-2026-04-04.md`
  - 实施方案：`/Users/zhouhao/.claude/plans/federated-singing-nebula.md`

---

## 里程碑定义

| 里程碑 | 内容 | 标志 |
|---|---|---|
| M1 | 项目骨架完成 | 数据库初始化、分层结构、依赖完整、能够启动服务 |
| M2 | 用户模块完成 | 注册、登录、Token 鉴权、个人设置可用 |
| M3 | 渠道模块完成 | 渠道 CRUD、6 个渠道驱动、可成功发送测试消息 |
| M4 | 通知模块完成 | 通知 CRUD、渠道绑定、状态机、Token 管理 |
| M5 | 执行层完成 | 调度、Worker、推送记录写入、Webhook 触发 |
| M6 | 外部调用完成 | Webhook 通知调用、渠道直连调用、Token 重置 |
| M7 | AI 模块完成 | 多轮会话、参数收集、通知生成 |
| M8 | 测试模块完成 | 双入口、示例代码生成 |
| M9 | 前端完成 | 所有页面串联、交互完成、样式到位 |
| M10 | 正式完成 | 全流程联调通过、Bug 修复、README 就绪 |

---

## 工时汇总

| 阶段 | 工时（人天） |
|---|---|
| 阶段一 | 3 |
| 阶段二 | 2 |
| 阶段三 | 3 |
| 阶段四 | 3 |
| 阶段五 | 1 |
| 阶段六 | 2 |
| 阶段七 | 2 |
| 阶段八 | 2 |
| 阶段九 | 1 |
| 阶段十 | 5 |
| 阶段十一 | 3 |
| **总计** | **27** |

---

## 注意事项

1. 数据库路径和 `/data/` 目录需在应用启动时自动初始化
2. SQLite 并发写入需注意 better-sqlite3 的序列化支持
3. node-schedule 的持久化恢复需在服务重启后正确重新调度
4. AI 多轮会话超时时间需有合理默认值
5. 建议每完成一个里程碑进行一次代码 review